<!-- views/followed-artists.handlebars -->
<div class="container">
    <h1>Followed Artist</h1>
    <ul>
        {{#each artists}}
            <li>
                <img src="{{this.images.[0].url}}" alt="{{this.name}}" width="50" height="50">
                <span>{{this.name}}</span>
            </li>
        {{/each}}
    </ul>
</div>